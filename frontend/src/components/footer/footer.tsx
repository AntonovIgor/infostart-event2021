import {Layout} from 'antd';

const {Footer: FooterLayout} = Layout;

export default function Footer():JSX.Element {
  return (
    <FooterLayout style={{ textAlign: 'center' }}>Infostart Event ©2021</FooterLayout>
  );
}
